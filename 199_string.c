// take string from user
//gets() // take string from user
// puts ()// display string on consol/output screen
#include <stdio.h>
void main()
{
    char name[30];
    printf("enter a string : ");
    gets(name);
    puts(name);
}