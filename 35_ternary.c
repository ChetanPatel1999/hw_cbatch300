// wap to check given two number rae same or differnt.
#include <stdio.h>
void main()
{
    int num1,num2;
    printf("enter two numbers : ");
    scanf("%d%d",&num1,&num2);
    num1 == num2 ? printf("number are same") : printf("number are diff");
}