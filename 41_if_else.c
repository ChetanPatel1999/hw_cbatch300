// wap to check given two nuber are same or differnet.
#include <stdio.h>
void main()
{
    int num1,num2;
    printf("enter two number  : ");
    scanf("%d%d", &num1,&num2); // 12 19
    if (num1==num2)
    {
        printf("nums are same");
    }
    else
    {
        printf("nums are different");
    }
}